---
slug: /settings
title: Settings
description: Configure your server's basic settings, language, timezone and emergency managers.
navigation.icon: 'twemoji:gear'
redactors: ['draftproducts']
updatedAt: '2026-09-02'
sourceHash: fcb3ab1af720667fee01cbfbeedd8233084c868e3761888152568bf4cee39abd
---

The **Settings** page of the panel gathers your server's basic settings. They apply to every **DraftBot** system.

You can access it by [**`clicking here`**](/dashboard/first/settings).

## Language and timezone

### Language

The language determines the one **DraftBot** uses in its messages and commands on your server. Two languages are available:

- **Français**
- **English**

Changing the language also updates the default messages of the various systems that have not been customized yet.

### Timezone

The timezone is used to correctly display dates and times wherever **DraftBot** handles them: [recurring messages](/docs/communication/recurring-messages), [birthdays](/docs/community/birthdays), logs, etc.

::hint{ type="info" }
  If your members are spread across several timezones, pick the one most of them live in: it is the one used for the times announced on the server.
::

## Emergency managers

When a serious issue is detected (disabled system, missing permission…), an urgent log is sent and the managers are mentioned so that the issue actually gets taken care of.

Two targets are available:

- **All administrators**: every member with the `Administrator` permission is mentioned.
- **Custom list**: you select the members to mention, up to 10.

::hint{ type="warning" }
  Pick managers who are actually active on the server: an ignored urgent log can leave an essential system out of order.
::
